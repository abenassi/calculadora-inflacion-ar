{
  "name": "calculadora-inflacion-ar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Calculadora de inflaci\u00f3n argentina con datos v\u00eda Argentina Data MCP",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "snapshot": "tsx scripts/fetch-snapshot.ts",
    "verificar": "tsc --noEmit && vitest run && vite build"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "chart.js": "^4.4.7"
  }
}
